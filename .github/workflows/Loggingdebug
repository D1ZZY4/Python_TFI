- name: Debug environment
  run: |
    echo "Python version:"
    python --version
    echo "Pip version:"
    pip --version
    echo "Installed packages:"
    pip freeze
    echo "Directory contents:"
    ls -al
    echo "Last 50 lines of script output:"
    tail -n 50 script-bot_TFI.py
